import styles from './about-content.module.scss';

// Placeholder — replace with real About copy.
export function AboutContent() {
  return (
    <div className={styles.about}>
      <p className={styles.placeholder}>Colby is a product forward engineering leader who has spent the last decade taking ambiguous ideas from sticky notes on a wall to first working prototype and beyond.</p>
    </div>
  );
}
